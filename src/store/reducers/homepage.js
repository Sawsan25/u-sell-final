// import * as types from "../actionTypes";

// cams -> campaigns
const initialState = {};

const homepage = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default homepage;
